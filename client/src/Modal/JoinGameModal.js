import React from 'react';

const JoinGameModal = ({}) => {
    const closeOnlineGameModal = () => {
        setShowOnlineGameModal(false);
    }
    return (
        <></>
    );
}

export default JoinGameModal;